<!DOCTYPE html>
<html>
  <head>
    <title>工科營查詢系統</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
	<span id="sizeA">老鼠暱稱: <%= data.name %></span><br/>
	<span id="sizeB">所屬小隊: <%= data.team %></span><br/>


    <%if(typeof(user) !== 'undefined' && user.username == '4ff75f80957469c4b6af5824cb99bf4919abad98'){%>
        <%if(user.comsumeMode){%>
          <br/>
            <form action="confirmConsume?value=<%= query.value %>&team=<%= data.team %>" method="post" id="check">
              <span id="sizeB"><font color="#C05050">
                <B>【消費
                 <input type="text" id="consumeValue" name="consumeValue" style="width:28px; font-family:Microsoft JhengHei">
                分】 小隊分數: <%= data.score %> → <span id="score">??</span>
                </B>
              </span>

              <button type="button" onclick="calc()"><span id="sizeB">計算分數</span></button>

              <script>
              function calc() {
                document.getElementById("score").innerHTML = <%= parseInt(data.score)%> - parseInt(document.getElementById("consumeValue").value);
                document.getElementById("hidden").style.display = "block";
              }
              </script>

              <span id="hidden" style="display: none"><input type="submit" value="確認消費" name="confirm" style=" font-family:Microsoft JhengHei"></span>
                
            </form>
          <br/>
          <a claa="btn" href="/index"><span id="sizeD">回首頁</span></a>
        <%}else{%>
        	<br/>
          <form action="confirmAdd?value=<%= query.value %>&team=<%= data.team %>" method="post">
          <span id="sizeB"><font color="#50C050"><B>【加分】 小隊分數: <%= data.score %> → 
            <%if(user.score){%>
            <%=parseInt(data.score) + parseInt(user.score)%>
            <%}else{%>
            <%=parseInt(data.score)%>
            <%} %>　</B></span>

            <input type="submit" value="確認加分" name="confirm" style=" font-family:Microsoft JhengHei"></form></span>
          </form>
        	<br/>
         <a claa="btn" href="/index"><span id="sizeD">回首頁</span></a>
         <%}%>
      <%}else{%>
        <span id="sizeC">小隊分數: <%= data.score %></span>
      <%}%>
  </body>
</html>